/**
 * This is to represents types of possible transactions.
 */
enum TransactionType {
  REGULAR = 1, //funds transfers and payments transctions
  FEE = 2, //fee payments and monetary expansion transactions
}

export default TransactionType;
